# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=x-ai/grok-4.1-fast

# MCP Connection Mode: 'remote' (primary) or 'local' (fallback)
MCP_MODE=remote

# Remote MCP Configuration (axion-planetary-mcp)
# Option 1: Local server (no auth, for testing)
MCP_REMOTE_URL=http://localhost:3000

# Option 2: Hosted server (with API key)
# MCP_REMOTE_URL=https://axion-mcp-sse.onrender.com
MCP_API_KEY=your-mcp-api-key-here

# Local MCP Configuration (fallback)
MCP_SERVER_PATH=dist/mcp/mcp-server.js

# Server Configuration
PORT=3001
NODE_ENV=development

# API Authentication (Temporary - until team implements OAuth)
# Comma-separated list of valid API keys
API_KEYS=dev-key-123,test-key-456,prod-key-789

# Database Configuration (Neon DB)
# Get connection string from: https://neon.tech
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Error Tracking (Sentry)
# Get DSN from: https://sentry.io
SENTRY_DSN=https://your-sentry-dsn-here
# Set to 'true' to enable Sentry in development
SENTRY_DEV_ENABLED=false

# Data Retention (Days)
# How long to keep API usage records before automatic cleanup
DATA_RETENTION_DAYS=30

# Application URL (for production)
APP_URL=http://localhost:3001

# Redis Configuration (Upstash - optional)
# Get credentials from: https://upstash.com
UPSTASH_REDIS_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_TOKEN=your-redis-token-here
